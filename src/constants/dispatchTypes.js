/* Display */
const SET_ACTIVE_MESSAGE = 'SET_ACTIVE_MESSAGE';
const SET_CASH_AMOUNT = 'SET_CASH_SUM';
export const displayTypes = {
    SET_ACTIVE_MESSAGE,
    SET_CASH_AMOUNT
};

/* Spinner */
const RUN_SPINNER = 'RUN_SPINNER';
const STOP_SPINNER = 'STOP_SPINNER';
export const spinnerTypes = {
    RUN_SPINNER,
    STOP_SPINNER
};
